type Option<T> = Some(T) | None

type Result<T, E> = Ok(T) | Err(E)

fn unwrap_or(o: Option<Int>, fallback: Int) -> Int {
    return match o {
        Some(x) => x,
        None    => fallback,
    };
}
